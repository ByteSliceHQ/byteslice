{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "//#ci:check": {},
    "//#ci:fix": { "cache": false },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
