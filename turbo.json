{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "//#code:check": {},
    "//#code:fix": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
